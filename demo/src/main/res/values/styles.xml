<resources>
    <!-- Keep legacy styles for compatibility (older layout ids may reference them). -->
    <style name="MaterialButton.Outlined" parent="Widget.MaterialComponents.Button.OutlinedButton">
        <item name="cornerRadius">12dp</item>
        <item name="strokeWidth">1dp</item>
        <item name="strokeColor">@color/ds_primary</item>
    </style>

    <style name="MaterialButton.Text" parent="Widget.MaterialComponents.Button.TextButton">
        <item name="cornerRadius">12dp</item>
    </style>
</resources>